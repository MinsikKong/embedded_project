<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <TextView
        android:id="@+id/cafeTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:text="POS게시판" />

    <WebView
        android:id="@+id/cafeView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/webBackButton" />

    <Button
        android:id="@+id/webBackButton"
        android:layout_width="wrap_content"
        android:layout_height="34dp"
        android:layout_alignRight="@+id/cafeTitle"
        android:layout_below="@+id/cafeTitle"
        android:text="back" />

    <Button
        android:id="@+id/webForwardButton"
        android:layout_width="wrap_content"
        android:layout_height="34dp"
        android:layout_below="@+id/cafeTitle"
        android:layout_toRightOf="@+id/cafeTitle"
        android:text="forward" />

</RelativeLayout>